<style>
    div {
        margin: auto;
        isolation: isolate;
        border-radius: 8px;
        padding: 16px;
        background-color: rgb(from var(--bg-color) r g b / .3);
        border: 1px solid rgb(from var(--bg-color) r g b / .5);
        backdrop-filter: blur(5px);
        max-width: 60ch;
    }
</style>

<div>
    <slot/>
</div>