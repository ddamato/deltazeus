<style>
    div {
        margin: auto;
        isolation: isolate;
        border-radius: 8px;
        padding: 16px;
        background-color: color(from currentColor xyz
            min((0.18 - y) * 10, alpha)
            min((0.18 - y) * 10, alpha)
            min((0.18 - y) * 10, alpha)
            / .3
        );
        border-width: 1px;
        border-style: solid;
        border-color: color(from currentColor xyz
            min((0.18 - y) * 10, alpha)
            min((0.18 - y) * 10, alpha)
            min((0.18 - y) * 10, alpha)
            / .5
        );
        backdrop-filter: blur(5px);
        max-width: 60ch;
    }
</style>

<div>
    <slot/>
</div>